To make XHTML/HTML5 file ready for dynamic previewing, drag and drop it on attach.vbs file to embed WICS code section to the content (the code references jquery, js and css files).

"Operation Complete" message indicates that the preparation completed successfully.

Make sure XHTML/HTML5 file contains <head></head> tags, otherwise the script will throw an error.