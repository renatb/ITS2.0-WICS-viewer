To convert one or more files into HTML files ready for hybrid preview,

1) copy this folder to some destination at you computer

2) copy the input files into some input folder

3) prepare some output folder (you can safely use the input folder as the output one)

4) edit the following sample batch conversion CLI file respectively:

	demo_batch_conversion.bat

5) run demo_batch_conversion.bat